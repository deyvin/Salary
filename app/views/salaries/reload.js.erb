<% if @salary.errors.any? %>
	$("#success").fadeOut(400);
	$("#error_message").html("<%= escape_javascript( render(partial: 'errors') ) %>");
	$("#errors").fadeIn(400);
<% else %>
	$("#errors").fadeOut(400);
	$("#success").fadeIn(400);

	$("form")[0].reset();
	$('.cidades')
	    .find('option')
	    .remove()
	    .end()
	    .append('<option value="none">Selecione Estado antes</option>');
	;

	$("#salarios").html("<%= escape_javascript( render(partial: 'list') ) %>")

	OSX.close();
<% end %>